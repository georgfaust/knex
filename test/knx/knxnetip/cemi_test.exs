defmodule CemiTest do
  use ExUnit.Case

  alias Knx.KnxnetIp.DataCemiFrame

  # test "temp" do
  #   assert 0 =
  #            DataCemiFrame.handle(<<0x29, 0x0, 0xBC, 0xE0, 0x11, 0xFF, 0x0, 0x1, 0x1, 0x0, 0x80>>)
  # end
end
